{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "nvcc",
            "args": ["-o", "output_file_name", "cuda_file.cu"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
